# ============================================================================
# APEX — environment overrides (all optional).
# The Cloudflare KV worker URL is baked in as a default; set VITE_APEX_KV_URL
# only if you deploy your own copy of the worker.
# ============================================================================
# VITE_APEX_KV_URL=https://apex-db.apexballtd-admin.workers.dev
